import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.intteko.prevencion2',
  appName: 'iCheckList',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
